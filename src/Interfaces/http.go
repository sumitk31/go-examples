package main

/*func main() {
	b1 := make([]byte, 100)
	var num int
	var err error
	resp, err := http.Get("https://google.com")
	fmt.Println("\n Error", err)
	num, err = resp.Body.Read(b1)
	fmt.Println(string(b1), err, num)
}*/
